# HEARTBEAT.md

Keep this file empty except for stable Nix-managed heartbeat policy. Dynamic heartbeat state belongs in `memory/heartbeat-state.json` or daily memory files.

Default behavior: reply `HEARTBEAT_OK` unless there is a specific useful update for Andy.

Useful heartbeat work:

- Check urgent inbox/calendar items only when configured and appropriate.
- Review recent memory and distill durable lessons into `MEMORY.md` during private main sessions.
- Notice stalled projects or uncommitted self/system changes that should go through Nix.

Quiet hours: avoid proactive messages from 23:00 to 08:00 Europe/Bucharest unless urgent.
